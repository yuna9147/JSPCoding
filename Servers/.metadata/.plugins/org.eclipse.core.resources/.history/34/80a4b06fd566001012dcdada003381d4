$("#submitBtn").on("click",function(){
	
	if($("#num1").val().replace(/\s/g,"")==""){
				alert("첫번째 숫자를 입력해 주세요");
				return;
			}
	
	if($("#num2").val().replace(/\s/g,"")==""){
					alert("두번째 숫자를 입력해 주세요");
					return;
				}		
	
	$("#frm").attr({
		"method":"post",
		"acttion":"/servletExample/calcServlet"
	})
})


$("#sendBtn").on("click", function(){
	if($("#word").val().replace("/\s/g","")===""){
		alert("검색 키워드를 입력해 주세요");
	}
	
	$("#form1").attr(action="response.sendRedirect(URL);")
	$("#form1").submit();
});
